---
import BaseLayout from './BaseLayout.astro'
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'

interface Props {
  title: string
  description: string
  canonical?: string
  ogType?: 'website' | 'article'
  ogImage?: string
  articleModified?: string
  schemaJson?: string
  bodyClass?: string
  /** Forwarded to BaseLayout; set false for pages that ship their own et-cache (e.g. retail 237824). */
  includeEtCache11Dynamic?: boolean
}

const { bodyClass = 'et_no_sidebar', ...props } = Astro.props
---

<BaseLayout {...props} bodyClass={bodyClass}>
  <Fragment slot="head">
    <slot name="head" />
  </Fragment>
  <Fragment slot="scripts">
    <slot name="scripts" />
  </Fragment>
  <Header />
  <slot />
  <Footer />
</BaseLayout>
